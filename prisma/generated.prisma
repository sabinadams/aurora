// *** GENERATED BY AURORA :: DO NOT EDIT ***
datasource db {
	provider = "postgresql"
	url = env("DATABASE_URL")
}
generator client {
	provider = "prisma-client-js"
}
model Post {
	id Int @id @default(autoincrement())
	createdAt DateTime @default(now())
	title String
	content String?
	published Boolean @default(false)
	author User?  @relation(name: "PostToUser", fields: [authorId], references: [id])
	authorId Int?
}
model User {
	personCreatedAt DateTime
	personParentId Int
	id Int @id @default(autoincrement())
	Person Person?  @relation(name: "PersonToUser", fields: [personCreatedAt,personParentId], references: [createdAt,parentId])
	name String?
	email String @unique
	posts Post[]  @relation(name: "PostToUser")
}
model Person {
	createdAt DateTime @default(now())
	parent User?  @relation(name: "PersonToUser")
	parentId Int @unique
	banana String
	@@id([createdAt, parentId])
}
enum Role {
	USER
	ADMIN @map("test")
}