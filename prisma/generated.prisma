// *** GENERATED BY AURORA :: DO NOT EDIT ***
datasource db {
	provider = "postgresql"
	url = env("DATABASE_URL")
}
generator client {
	provider = "prisma-client-js"
}
model Post {
	id Int @id @default(autoincrement())
	createdAt DateTime @default(now())
	title String
	content String?
	published Boolean @default(false)
	author User?  @relation(name: "PostToUser", fields: [authorId], references: [id])
	authorId Int?
}
model User {
	personId Int
	id Int @id @default(autoincrement())
	Person Person?  @relation(name: "PersonToUser", fields: [personId], references: [id])
	name String?
	email String @unique
	posts Post[]  @relation(name: "PostToUser")
}
model Person {
	id Int @id @default(autoincrement())
	createdAt DateTime @default(now())
	parent User?  @relation(name: "PersonToUser")
	parentId Int?
	banana String
}
enum Role {
	USER
	ADMIN @map("test")
}